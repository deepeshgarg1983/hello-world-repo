name: Matrix config
'on': push
      workflow_dispatch:
jobs:
  deploy-on-ubuntu:
    runs-on: ubuntu-latest
    steps:
      - name: Echo docker info
        run: docker info
   deploy-on-ubuntu:
     runs-on: windows-latest
    steps:
      - name: Echo docker details
        run: docker info

