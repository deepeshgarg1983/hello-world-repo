name: Matrix config
on: push

jobs:
  deploy-on-ubuntu:
    runs-on: ubuntu-latest
    steps:
      - name: Echo docker info
        run: docker info

  deploy-on-windows:
    runs-on: windows-latest
    steps:
      - name: Echo docker details
        run: docker info


